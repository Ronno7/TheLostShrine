// Run in a fresh PrototypeLoop session with an empty, isolated CheckpointSession saveKey.
// These geometry/lifecycle checks refill stamina between simulated segments;
// StaminaPlayModeChecks separately tests action costs and exhaustion.
// Fixtures use empty space outside the map; never run against a player's saved route.
var results=new System.Collections.Generic.List<string>();
System.Action<bool,string> check=(ok,name)=>results.Add((ok?"PASS ":"FAIL ")+name);
var player=GameObject.Find("Player").GetComponent<TheLostShrine.Player.PlayerCombatController>();
var weapon=GameObject.Find("Hatchet Pickup").GetComponent<TheLostShrine.Weapons.HatchetWeapon>();
var stamina=player.GetComponent<TheLostShrine.Player.PlayerStamina>();
var body=player.GetComponent<Rigidbody2D>();
var sim=typeof(TheLostShrine.Weapons.HatchetWeapon).GetMethod("Simulate",System.Reflection.BindingFlags.Instance|System.Reflection.BindingFlags.NonPublic);
System.Action<float> tick=duration=>{int steps=Mathf.CeilToInt(duration/.02f);for(int i=0;i<steps;i++)sim.Invoke(weapon,new object[]{.02f});stamina.Restore();};
System.Action<Vector2> move=position=>{body.position=position;body.linearVelocity=Vector2.zero;player.transform.position=position;Physics2D.SyncTransforms();};
var created=new System.Collections.Generic.List<GameObject>();
System.Func<string,Vector2,GameObject> spawn=(path,position)=>{var go=UnityEngine.Object.Instantiate(AssetDatabase.LoadAssetAtPath<GameObject>(path));go.name="Verification Target";go.transform.position=position;var rb=go.GetComponent<Rigidbody2D>();if(rb!=null)rb.position=position;created.Add(go);Physics2D.SyncTransforms();return go;};
string dummyPath="Assets/Prefabs/Combat/PracticeDummy.prefab";
var oldSimulation=Physics2D.simulationMode;
var start=body.position;
var view=weapon.GetComponent<TheLostShrine.Weapons.HatchetView>();
var indicator=weapon.GetComponent<TheLostShrine.Weapons.HatchetComboIndicator>();
var viewUpdate=view.GetType().GetMethod("LateUpdate",System.Reflection.BindingFlags.Instance|System.Reflection.BindingFlags.NonPublic);
var indicatorUpdate=indicator.GetType().GetMethod("LateUpdate",System.Reflection.BindingFlags.Instance|System.Reflection.BindingFlags.NonPublic);
System.Action render=()=>{viewUpdate.Invoke(view,null);indicatorUpdate.Invoke(indicator,null);};
var slash=weapon.GetComponentInChildren<LineRenderer>();
var model=weapon.transform.Find("Model");
var pips=weapon.transform.Find("Combo Indicator").GetComponentsInChildren<SpriteRenderer>();
try{
Physics2D.simulationMode=SimulationMode2D.Script;
check(player.Weapon==null && weapon.State==TheLostShrine.Weapons.HatchetState.OnGround,"Starts unarmed with grounded prefab");
render();check(pips.All(p=>!p.enabled),"Combo indicator is hidden before pickup");
check(!weapon.TryThrow(Vector2.right) && !weapon.TryLightChop(Vector2.right),"Ground weapon rejects attacks");
move(weapon.transform.position);Physics2D.Simulate(.02f);
check(player.Weapon==weapon && weapon.State==TheLostShrine.Weapons.HatchetState.Held && !weapon.GetComponent<Collider2D>().enabled,"Real trigger picks up the existing hatchet");
check(!player.CanRecall,"Pickup leaves Recall locked");
check(!player.TryEquip(weapon),"Duplicate equip rejected");
Vector2 origin=new Vector2(100,100);move(origin);
var dummy=spawn(dummyPath,origin+Vector2.right);
var health=dummy.GetComponent<TheLostShrine.Combat.Damageable>();
var extra=new GameObject("Extra hit collider");extra.transform.SetParent(dummy.transform,false);extra.AddComponent<CircleCollider2D>().radius=.25f;
for(int i=0;i<3;i++){
check(weapon.TryLightChop(Vector2.right) && weapon.ComboIndex==i,"Combo stage "+(i+1));
check(!weapon.TryThrow(Vector2.up) && !weapon.TryBeginCharge(),"Action exclusivity during chop "+(i+1));
int before=health.Health;tick(.02f);render();
check(health.Health==before && !slash.enabled,"Windup has no damage or slash effect, stage "+(i+1));
tick(.06f);render();
Vector2 tail=slash.GetPosition(0)-weapon.transform.position;Vector2 head=slash.GetPosition(slash.positionCount-1)-weapon.transform.position;
float signedSweep=Vector2.SignedAngle(tail,head);
check(slash.enabled && (i==1?signedSweep<0:signedSweep>0) && Vector2.Dot(head.normalized,((Vector2)model.localPosition).normalized)>.999f,"Slash follows blade direction, stage "+(i+1));
check(pips.Length==3 && pips.All(p=>p.enabled) && pips[i].color.a>pips[(i+1)%3].color.a && weapon.ComboStep==i+1,"Pips show the active combo step "+(i+1));
check(Mathf.Abs(weapon.LightDuration-(i==2?.23f:.2f))<.001f,"Shorter attack duration, stage "+(i+1));
tick(.26f);render();
check(!slash.enabled && weapon.ComboStep==i+1,"Slash clears while combo indicator remains available, stage "+(i+1));
}
check(health.Health==65,"Three-hit combo deals 10/10/15, once per target despite multiple colliders");
tick(.7f);render();check(weapon.ComboStep==0 && pips.All(p=>!p.enabled),"Combo timeout clears all pips");
check(weapon.TryLightChop(Vector2.right) && weapon.ComboIndex==0,"Combo resets after timeout");tick(.34f);
dummy.SetActive(false);
var lateTarget=spawn(dummyPath,origin+Vector2.right*4);
weapon.TryLightChop(Vector2.right);tick(.16f);
lateTarget.GetComponent<Rigidbody2D>().position=origin+Vector2.right;lateTarget.transform.position=origin+Vector2.right;Physics2D.SyncTransforms();tick(.1f);
check(lateTarget.GetComponent<TheLostShrine.Combat.Damageable>().Health==100,"An enemy entering range during recovery takes no lingering slash damage");lateTarget.SetActive(false);
var behind=spawn(dummyPath,origin+Vector2.left);
weapon.TryLightChop(Vector2.right);tick(.34f);
check(behind.GetComponent<TheLostShrine.Combat.Damageable>().Health==100,"Directional chop excludes targets behind");
weapon.TryBeginCharge();tick(.1f);check(!weapon.TryReleaseCharge(Vector2.right) && weapon.State==TheLostShrine.Weapons.HatchetState.Held,"Tap below minimum charge cancels");
weapon.TryBeginCharge();tick(.9f);check(weapon.Charge01==1f,"Charge clamps at full strength");
render();check(pips.All(p=>!p.enabled) && Mathf.Abs(slash.widthMultiplier-.045f)<.001f,"Charging hides combo pips and restores the thin charge ring");
weapon.TryReleaseCharge(Vector2.right);tick(.48f);
check(behind.GetComponent<TheLostShrine.Combat.Damageable>().Health==70,"Full cleave hits behind for 30 damage");
check(behind.GetComponent<TheLostShrine.Combat.HitReaction>().IsStaggered && behind.GetComponent<Rigidbody2D>().linearVelocity.x<0,"Cleave applies outward knockback and stagger");
behind.SetActive(false);
var guarded=spawn("Assets/Prefabs/Combat/ShieldPracticeDummy.prefab",origin+Vector2.right);
var guardHealth=guarded.GetComponent<TheLostShrine.Combat.Damageable>();var shield=guarded.GetComponent<TheLostShrine.Combat.ShieldProtection>();
weapon.TryLightChop(Vector2.right);tick(.34f);
weapon.TryBeginCharge();tick(.3f);weapon.TryReleaseCharge(Vector2.right);tick(.48f);
check(guardHealth.Health==100 && shield.IsIntact,"Shield blocks light and partial cleave");
weapon.TryBeginCharge();tick(.9f);weapon.TryReleaseCharge(Vector2.right);tick(.48f);
check(guardHealth.Health==70 && !shield.IsIntact,"Full cleave breaks shield and damages target");
guarded.SetActive(false);
var bush=spawn("Assets/Prefabs/World/Bush.prefab",origin+Vector2.right);
weapon.TryLightChop(Vector2.right);tick(.34f);
check(!bush.activeSelf,"Light chop breaks bushes");
var stone=spawn("Assets/Prefabs/World/CrackedStone.prefab",origin+Vector2.right);
weapon.TryLightChop(Vector2.right);tick(.34f);check(stone.activeSelf,"Cracked stone resists light chop");
weapon.TryBeginCharge();tick(.9f);weapon.TryReleaseCharge(Vector2.right);tick(.48f);check(!stone.activeSelf,"Full cleave breaks cracked stone");
weapon.TryThrow(Vector2.right);check(!weapon.TryRecall(),"Locked Recall rejects in-flight recall");
render();check(pips.All(p=>!p.enabled),"Throwing clears the combo indicator");
check(!weapon.TryLightChop(Vector2.right) && !weapon.TryBeginCharge(),"Melee unavailable while weapon is away");tick(.7f);
check(weapon.State==TheLostShrine.Weapons.HatchetState.Stuck && Mathf.Abs(Vector2.Distance(origin,weapon.transform.position)-6)<.05f,"Throw stops at configured range");
check(!weapon.TryRecall(),"Locked Recall rejects stuck recall");
move(weapon.transform.position);tick(.02f);check(weapon.State==TheLostShrine.Weapons.HatchetState.Held,"Walk-up retrieval works before Recall unlock");
move(UnityEngine.Object.FindFirstObjectByType<TheLostShrine.World.RecallUnlockPickup>().transform.position);Physics2D.Simulate(.02f);check(player.CanRecall,"Separate altar trigger unlocks Recall");
move(origin);
var obstacle=new GameObject("Verification wall");obstacle.transform.position=origin+Vector2.right*2;obstacle.AddComponent<BoxCollider2D>().size=new Vector2(.2f,2);created.Add(obstacle);Physics2D.SyncTransforms();
weapon.TryThrow(Vector2.right);tick(.3f);check(weapon.State==TheLostShrine.Weapons.HatchetState.Stuck && weapon.transform.position.x<origin.x+1.8f,"Swept throw stops at solid obstacle");
weapon.TryRecall();tick(.5f);check(weapon.State==TheLostShrine.Weapons.HatchetState.Held,"Recall returns from obstacle");
var blockedTarget=spawn(dummyPath,origin+Vector2.right*2.7f);
var detector=new TheLostShrine.Weapons.HatchetHitDetector(player.transform,weapon.transform);
detector.Melee(origin,Vector2.right,4,120,new TheLostShrine.Combat.CombatHit(player.gameObject,TheLostShrine.Combat.AttackKind.LightChop,1,Vector2.right,0,0));
check(blockedTarget.GetComponent<TheLostShrine.Combat.Damageable>().Health==100,"Melee cannot hit through solid terrain");
obstacle.SetActive(false);blockedTarget.SetActive(false);
var flightTarget=spawn(dummyPath,origin+Vector2.right*2);
weapon.TryThrow(Vector2.right);tick(.22f);check(flightTarget.GetComponent<TheLostShrine.Combat.Damageable>().Health==85 && weapon.State==TheLostShrine.Weapons.HatchetState.Stuck,"Outbound throw damages once and sticks to target");
weapon.TryRecall();check(!weapon.TryRecall(),"Repeated recall cannot restart return");tick(.4f);
check(flightTarget.GetComponent<TheLostShrine.Combat.Damageable>().Health==75,"Same target can be hit once outbound and once returning");
flightTarget.SetActive(false);
weapon.TryThrow(Vector2.right);tick(.7f);
var returnA=spawn(dummyPath,origin+Vector2.right*2);var returnB=spawn(dummyPath,origin+Vector2.right*4);
obstacle.SetActive(true);Physics2D.SyncTransforms();
weapon.TryRecall();tick(.14f);move(origin+Vector2.down*.2f);tick(.6f);
check(returnA.GetComponent<TheLostShrine.Combat.Damageable>().Health==90 && returnB.GetComponent<TheLostShrine.Combat.Damageable>().Health==90,"Returning hatchet pierces multiple targets once each");
check(weapon.State==TheLostShrine.Weapons.HatchetState.Held && Vector2.Distance(weapon.transform.position,body.position)<.1f,"Return crosses terrain and tracks moving owner");
returnA.SetActive(false);returnB.SetActive(false);obstacle.SetActive(false);
move(origin);var doomed=spawn(dummyPath,origin+Vector2.right*2);
weapon.TryThrow(Vector2.right);tick(.22f);UnityEngine.Object.DestroyImmediate(doomed);tick(.1f);
check(weapon!=null && weapon.State==TheLostShrine.Weapons.HatchetState.Stuck,"Destroying struck target preserves the hatchet");
weapon.TryRecall();tick(.5f);
// Recall bypass depends on the contact side, while leaving the shield intact.
Vector2 guardPosition=origin+Vector2.left*4;
var rearTarget=spawn("Assets/Prefabs/Combat/ShieldPracticeDummy.prefab",guardPosition);
var rearHealth=rearTarget.GetComponent<TheLostShrine.Combat.Damageable>();
var rearShield=rearTarget.GetComponent<TheLostShrine.Combat.ShieldProtection>();
var facing=rearTarget.transform.Find("Facing");
int blockedCount=0;int brokenCount=0;int acceptedCount=0;
TheLostShrine.Combat.CombatHit acceptedHit=default(TheLostShrine.Combat.CombatHit);
rearShield.Blocked+=()=>blockedCount++;
rearShield.Broken+=()=>brokenCount++;
rearHealth.HitReceived+=hit=>{acceptedCount++;acceptedHit=hit;};
var extraRearCollider=new GameObject("Additional target collider");extraRearCollider.transform.SetParent(rearTarget.transform,false);extraRearCollider.AddComponent<CircleCollider2D>().radius=.25f;
System.Func<TheLostShrine.Combat.AttackKind,Vector2,bool,TheLostShrine.Combat.CombatHit> contact=(kind,offset,breakGuard)=>new TheLostShrine.Combat.CombatHit(player.gameObject,kind,2,Vector2.down,0,0,breakGuard,guardPosition+offset);
check(rearShield.FacingDirection==Vector2.down,"Shield front matches its south-facing marker");
check(!rearShield.IsRearRecallHit(contact(TheLostShrine.Combat.AttackKind.Recall,Vector2.down,false)),"Front Recall contact stays guarded");
check(!rearShield.IsRearRecallHit(contact(TheLostShrine.Combat.AttackKind.Recall,Vector2.left,false)) && !rearShield.IsRearRecallHit(contact(TheLostShrine.Combat.AttackKind.Recall,Vector2.right,false)),"Exact side contacts stay guarded");
check(rearShield.IsRearRecallHit(contact(TheLostShrine.Combat.AttackKind.Recall,Vector2.up+Vector2.right,false)),"Oblique contact in rear half is vulnerable");
check(!rearShield.IsRearRecallHit(new TheLostShrine.Combat.CombatHit(player.gameObject,TheLostShrine.Combat.AttackKind.Recall,2,Vector2.down,0,0)) && !rearShield.IsRearRecallHit(contact(TheLostShrine.Combat.AttackKind.Recall,Vector2.zero,false)),"Missing or central contact cannot bypass protection");
rearHealth.ReceiveHit(contact(TheLostShrine.Combat.AttackKind.LightChop,Vector2.up,false));
rearHealth.ReceiveHit(contact(TheLostShrine.Combat.AttackKind.Throw,Vector2.up,false));
rearHealth.ReceiveHit(contact(TheLostShrine.Combat.AttackKind.ChargedCleave,Vector2.up,false));
check(rearHealth.Health==100 && rearShield.IsIntact,"Rear light, outbound throw, and partial cleave remain blocked");
move(guardPosition+Vector2.down*2);
var rearDetector=new TheLostShrine.Weapons.HatchetHitDetector(player.transform,weapon.transform);
System.Action<Vector2,Vector2> recallPass=(from,to)=>{rearDetector.BeginAttack();Physics2D.SyncTransforms();RaycastHit2D unused;rearDetector.Flight(from,to,.18f,new TheLostShrine.Combat.CombatHit(player.gameObject,TheLostShrine.Combat.AttackKind.Recall,2,(to-from).normalized,1.5f,.2f),false,out unused);};
recallPass(guardPosition+Vector2.up*2,guardPosition+Vector2.down*2);
check(rearHealth.Health==98 && acceptedCount==1 && rearShield.IsIntact,"Swept rear Recall bypasses shield once, including multiple colliders");
check(brokenCount==0 && blockedCount==3,"Rear bypass emits no block or shield-break event");
check(acceptedHit.ImpactPoint.HasValue && acceptedHit.ImpactPoint.Value.y>guardPosition.y,"Flight delivers the actual rear impact point");
check(acceptedHit.Knockback==1.5f && acceptedHit.StaggerDuration==.2f,"Rear hit preserves ordinary Recall reaction without a new stun");
rearHealth.RestoreHealth();
recallPass(guardPosition+Vector2.down*2,guardPosition+Vector2.up*2);
check(rearHealth.Health==100 && blockedCount==4,"Swept front Recall is blocked even when it passes through");
facing.localRotation=Quaternion.Euler(0,0,90);
check(Vector2.Dot(rearShield.FacingDirection,Vector2.right)>.999f,"Turning the facing reference updates shield orientation");
recallPass(guardPosition+Vector2.left*2,guardPosition+Vector2.right*2);
check(rearHealth.Health==98 && rearShield.IsIntact,"Rear Recall follows rotated enemy facing");
rearHealth.RestoreHealth();facing.localRotation=Quaternion.identity;
move(guardPosition+new Vector2(1,-2));weapon.TryThrow(Vector2.up);tick(.7f);
check(weapon.State==TheLostShrine.Weapons.HatchetState.Stuck && rearHealth.Health==100,"Throw alongside guard reaches a position behind it");
move(guardPosition+Vector2.down*2);weapon.TryRecall();tick(.7f);
check(rearHealth.Health==90 && rearShield.IsIntact && weapon.State==TheLostShrine.Weapons.HatchetState.Held,"Real throw, reposition, and Recall damages rear without breaking shield");
rearHealth.RestoreHealth();
move(guardPosition+new Vector2(1,2));weapon.TryThrow(Vector2.down);tick(.7f);
move(guardPosition+Vector2.up*2);weapon.TryRecall();tick(.7f);
check(rearHealth.Health==100 && rearShield.IsIntact && weapon.State==TheLostShrine.Weapons.HatchetState.Held,"Real Recall from the front remains blocked");
rearHealth.ReceiveHit(contact(TheLostShrine.Combat.AttackKind.ChargedCleave,Vector2.down,true));
check(rearHealth.Health==98 && !rearShield.IsIntact && brokenCount==1,"Full cleave still breaks the guard after rear bypass");
rearHealth.ReceiveHit(contact(TheLostShrine.Combat.AttackKind.Recall,Vector2.down,false));
check(rearHealth.Health==96,"Broken shield no longer blocks front Recall");
rearTarget.GetComponent<TheLostShrine.Prototype.PracticeTarget>().ResetTarget();
check(rearHealth.Health==100 && rearShield.IsIntact,"Practice reset restores health and protection after rear hits");
rearTarget.SetActive(false);
check(UnityEngine.Object.FindObjectsByType<TheLostShrine.Weapons.HatchetWeapon>(FindObjectsSortMode.None).Length==1,"Single weapon instance throughout lifecycle");
}finally{
foreach(var go in created)if(go!=null)UnityEngine.Object.DestroyImmediate(go);
move(start);Physics2D.simulationMode=oldSimulation;
}
return string.Join("\n",results.ToArray());
